@model IEnumerable<Superhero_Creator.Models.Superhero>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    
        <table> class="table table-striped table-hover"
             <thead>
                <tr>
                    <th>#</th>
                    <th> @Html.DisplayNameFor(model => model.Name)</th>
                    <th>Other Stuff</th>
                   
                </tr>
            </thead>
             <tbody>

                 @foreach (var item in Model)
                 {
                     <tr class="warning">
                         <td>
                             <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-link">@Html.DisplayFor(modelItem => item.Id)</a>
                         </td>
                         <td>
                             <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-link">@Html.DisplayFor(modelItem => item.Name)</a>
                         </td>
                         <td>
                             <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                             <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                             <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>

                         </td>
                        
                     </tr>
                    
                 }
             </tbody>
   

        </table>
            

